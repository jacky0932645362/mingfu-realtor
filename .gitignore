node_modules/
.next/
out/
build/

# 🔴 環境變數：裡面有資料庫密碼與 Google 憑證，絕對不要進版控
.env
.env.local
.env*.local
.env.production

# 🔴 只給本機看樣子用的暫時頁，絕對不要上線 —— 部署出去等於在正式站
#    多開一個 /dev-card-preview 網址，客戶或 Google 都可能撞見。
/src/app/dev-card-preview/

# 本機開發伺服器的啟動設定，只對這台電腦有意義
/.claude/

# maplibre-gl 的背景工作程式，由 predev/prebuild 自動從 node_modules 複製產生
# （見 scripts/sync-maplibre-worker.mjs），跟 .next/ 一樣是「產出物」不是原始碼，不用進版控
/public/maplibre/

*.log
npm-debug.log*
.DS_Store
Thumbs.db
.vercel
*.tsbuildinfo
next-env.d.ts
/prisma/*.db
